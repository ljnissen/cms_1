<% provide(:title, 'Subjects') %>
<h1>Subjects</h1>

<ul class="subjects">
	<%= render @subjects %>
</ul>
	<% if current_user.admin? %>
		<%= link_to 'New', new_subject_path(@subjects), class: 'btn btn-primary' %>
	<% end %>